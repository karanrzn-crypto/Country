/**
 * Player state slice — the player's country, current mode, focus and selection.
 * Persisted with saves so the player resumes exactly where they left off.
 */

import type { PlayerSlice } from '../../player/types';

export type { PlayerSlice };
